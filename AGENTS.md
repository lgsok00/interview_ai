# Interview AI Backend 작업 지침

## 협업 방식

- 애플리케이션 코드는 Codex가 현재 코드를 분석한 뒤 변경할 파일, 코드, 적용 순서와 이유를 제시하고, 사용자가 직접 작성하여 반영한다. 새로 생성하는 파일은 package 선언과 import를 포함한 전체
  코드를 제공한다. 기존 파일을 수정하는 경우에는 변경 위치를 명확히 밝히고 수정되는 부분만 제공하며, 변경되지 않는 코드는 반복해서 보여주지 않는다.
- 기존 파일의 수정 코드를 제시할 때는 대상 파일 경로와 클래스·메서드 등 변경 위치를 명시하고, 사용자가 정확히 적용할 수 있도록 필요한 전후 문맥을 함께 제공한다. 새 파일의 전체 코드에는 `...` 등의 생략
  표현을 사용하지 않는다.
- Codex는 사용자가 명시적으로 애플리케이션 코드 수정을 요청하지 않은 한 애플리케이션 코드를 직접 수정하지 않는다.
- 사용자가 애플리케이션 코드 작성과 반영을 완료하면, Codex는 해당 변경사항을 확인한 뒤 정상·경계·실패 시나리오를 고려한 테스트 코드를 직접 작성하여 저장소에 반영한다.
- Codex는 테스트를 직접 실행하지 않고, 사용자가 실행할 수 있도록 저장소의 Gradle Wrapper를 사용하는 테스트 명령어만 제시한다.
- 사용자가 구현 완료와 테스트 실행 결과를 전달하면 Codex는 실제 저장소 변경사항과 전달받은 테스트 결과를 확인한 후 `docs/PROJECT_STATUS.md`와 작업에 관련된 상세 문서를 업데이트한다.
- `docs/PROJECT_STATUS.md` 업데이트가 끝나면 Codex는 해당 변경사항에 적합한 Git 커밋 메시지와 상세 커밋 메시지를 제시한다.
- 구현 또는 테스트가 확인되지 않은 항목은 완료로 기록하지 않는다.
- 작업 현황 문서만 업데이트하는 경우에도 실제 코드와 Git 상태를 먼저 확인한다.

## 프로젝트 기준

- Java 21과 저장소의 Gradle Wrapper를 사용한다.
- macOS/Linux에서는 `./gradlew`, Windows에서는 `.\gradlew.bat`을 사용한다.
- Spring Boot, Spring Security, JPA, Flyway 기반 구조를 유지한다.
- DB 스키마 변경은 기존 migration 수정이 아니라 새 Flyway migration으로 추가한다.
- 인증정보, 비밀번호, JWT secret 및 `.env` 파일을 커밋하지 않는다.
- 이메일은 저장 및 조회 전에 소문자 변환과 앞뒤 공백 제거를 유지한다.
- API 오류는 `GlobalExceptionHandler`와 `ErrorResponse` 형식을 따른다.
- 기능 변경에는 정상·경계·실패 시나리오 테스트를 함께 제안한다.

## 개발 시작 시 문서 참고

- 새 세션이나 개발 재개 시 [프로젝트 현황](docs/PROJECT_STATUS.md)을 먼저 읽고 현재 구현 범위, 최신 검증, 다음 작업과 Git 기준점을 확인한다.
- 이어서 아래 문서 중 작업과 관련된 내용을 읽고 실제 코드·테스트·Flyway migration 및 Git 상태와 대조한 뒤 변경안을 제시한다. 모든 상세 문서를 매번 읽을 필요는 없다.

| 문서                                        | 참고할 때                                        |
|-------------------------------------------|----------------------------------------------|
| [구현 상세](docs/status/IMPLEMENTATION.md)    | 실행 환경, 인증·사용자·자기소개서·이력서 API와 DB 구조를 확인할 때    |
| [기업·채용공고](docs/status/CATALOG.md)         | 기업·공고 API, 관리자 권한, 검색·상태·삭제·동시성 정책을 다룰 때     |
| [자동 테스트 목록](docs/status/TEST_COVERAGE.md) | 기존 검증 범위를 확인하고 추가할 정상·경계·실패 테스트를 정할 때        |
| [테스트 실행 기록](docs/status/TEST_RESULTS.md)  | 실행 명령·날짜·결과와 검증 대기 여부, RAG의 검증된 구현 범위를 확인할 때 |
| [개발 로드맵](docs/status/ROADMAP.md)          | 개발 순서, 선행 작업과 후속 범위를 정할 때                    |
| [주요 결정과 확인 사항](docs/status/DECISIONS.md)  | API·인증·운영 정책과 미해결 사항을 확인할 때                  |
| [변경 이력](docs/status/HISTORY.md)           | 이전 결정의 배경과 상태 변경 경위를 확인할 때                   |

- 과거 기록의 `현재`, `다음 작업`, `검증 대기`는 기록 당시 표현이다. 최신 상태는 `docs/PROJECT_STATUS.md`와 날짜가 명시된 최신 검증 기록을 기준으로 판단한다.
- 문서와 실제 저장소가 다르면 차이를 명시하고 코드·Git 상태·확인된 테스트 결과에 근거해 정리한다. 문서의 완료 표시만으로 구현 또는 검증 완료를 추정하지 않는다.

## 작업 현황 관리

- 지속적인 프로젝트 상태의 기준 문서는 `docs/PROJECT_STATUS.md`이다.
- 기준 문서에는 현재 상태·다음 작업·최신 검증 요약·실행 환경·Git 기준점과 상세 문서 링크를 유지한다. 긴 구현 설명이나 누적 테스트 기록을 다시 모으지 않는다.
- 구현 상세는 `IMPLEMENTATION.md`, 기업·채용공고 정책은 `CATALOG.md`, 테스트 범위는 `TEST_COVERAGE.md`, 실행 결과는 `TEST_RESULTS.md`, 전체 개발 순서는
  `ROADMAP.md`, 공통 결정·미해결 사항은 `DECISIONS.md`, 변경 이력은 `HISTORY.md`에 반영한다. 이 파일들은 모두 `docs/status/` 아래에 있다.
- 작업으로 달라진 내용이 있는 상세 문서만 갱신하고 기준 문서의 요약과 일치시킨다. 새 상세 문서를 추가하거나 경로를 바꾸면 기준 문서와 이 지침의 링크도 함께 갱신한다.
- 완료된 기능, 테스트 결과, 진행 중 작업, 다음 작업, 주요 결정, 실행 환경 변경을 반영한다.
- 테스트 결과에는 실행 명령, 실행 날짜, 성공 여부를 기록한다.
- 코드만 작성되고 테스트가 완료되지 않았다면 `구현됨, 검증 대기`로 구분한다.
- 과거 기록을 임의로 삭제하지 말고, 중요한 설계 변경은 변경 이력에 짧게 남긴다.
