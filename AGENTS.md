# Interview AI Backend 작업 지침

## 협업 방식

- 사용자가 애플리케이션 코드와 테스트 코드를 직접 작성한다.
- Codex는 구현 요청을 받으면 우선 현재 코드를 분석하고, 사용자가 직접 적용할 수 있도록 변경할 파일, 코드, 적용 순서와 이유를 제시한다.
- 사용자가 명시적으로 파일 수정을 요청하지 않은 한 Codex는 애플리케이션 코드와 테스트 코드를 직접 수정하지 않는다.
- 사용자가 구현과 테스트 완료를 알리면 Codex는 실제 저장소 변경사항과 사용자가 전달한 테스트 결과를 확인한 후 `docs/PROJECT_STATUS.md`를 업데이트한다.
- 구현 또는 테스트가 확인되지 않은 항목은 완료로 기록하지 않는다.
- 작업 현황 문서만 업데이트하는 경우에도 실제 코드와 Git 상태를 먼저 확인한다.

## 프로젝트 기준

- Java 21과 저장소의 Gradle Wrapper를 사용한다.
- macOS/Linux에서는 `./gradlew`, Windows에서는 `.\gradlew.bat`을 사용한다.
- Spring Boot, Spring Security, JPA, Flyway 기반 구조를 유지한다.
- DB 스키마 변경은 기존 migration 수정이 아니라 새 Flyway migration으로 추가한다.
- 인증정보, 비밀번호, JWT secret 및 `.env` 파일을 커밋하지 않는다.
- 이메일은 저장 및 조회 전에 소문자 변환과 앞뒤 공백 제거를 유지한다.
- API 오류는 `GlobalExceptionHandler`와 `ErrorResponse` 형식을 따른다.
- 기능 변경에는 정상·경계·실패 시나리오 테스트를 함께 제안한다.

## 작업 현황 관리

- 지속적인 프로젝트 상태의 기준 문서는 `docs/PROJECT_STATUS.md`이다.
- 완료된 기능, 테스트 결과, 진행 중 작업, 다음 작업, 주요 결정, 실행 환경 변경을 반영한다.
- 테스트 결과에는 실행 명령, 실행 날짜, 성공 여부를 기록한다.
- 코드만 작성되고 테스트가 완료되지 않았다면 `구현됨, 검증 대기`로 구분한다.
- 과거 기록을 임의로 삭제하지 말고, 중요한 설계 변경은 변경 이력에 짧게 남긴다.

